# Starter code

This directory contains the completed Hoot applications before the AI Helper
feature is added:

- `hoot-frontend`: React and Vite
- `hoot-backend`: Django REST Framework

Copy the applications into separate working repositories or use the completed
class applications as the starting point. Create local `.env` files from each
`.env.example`; never commit the local environment files.
